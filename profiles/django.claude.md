#### Backend: Django

- **Location**: `{directory}/`
- **Stack**: Python 3.12+, Django 5.x, Django REST Framework
- **Key patterns**: the `django-patterns` skill (auto-loads; installed at `.claude/skills/django-patterns/SKILL.md`)
- **Testing**: pytest with Django test client
- **Integration**: After API changes, generate integration summaries in `docs/integration/`
