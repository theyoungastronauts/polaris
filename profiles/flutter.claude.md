#### Frontend: Flutter

- **Location**: `{directory}/`
- **Stack**: Dart, Flutter
- **Key patterns**: the `flutter-patterns` skill (auto-loads; installed at `.claude/skills/flutter-patterns/SKILL.md`)
- **API consumption**: Check integration summaries in `docs/integration/` for endpoint contracts
